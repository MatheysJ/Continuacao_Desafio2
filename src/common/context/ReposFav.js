/* import { createContext, useState } from "react";

export const ReposFavContext = createContext();
ReposFavContext.displayName = "Repositórios Favoritos";

export const ReposFavProvider = ({ children }) => {
    const [ reposFav, setReposFav ] = useState("");

    return (
        <ReposFavContext.Provider value={{ reposFav, setReposFav}}>
            { children }
        </ReposFavContext.Provider>
    )
} */